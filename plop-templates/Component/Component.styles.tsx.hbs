import { css } from '@emotion/css';
import {
  BLACK,
  /* add the colors you need to import */
} from '../../utils/colors';

const base = css`
  /* start adding your shared styles here */
`;

export const primary = css`
  ${base}

  /* create all the variants you need and export them */
`;